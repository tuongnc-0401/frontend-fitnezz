import { makeStyles } from "@material-ui/core";

export default makeStyles((theme) => ({
    container: {
        width: '100%',
        display: 'flex',
    },

    plankSpace: {
        height: '400px',
        minWidth: '240px',
        backgroundColor: 'aqua'
    }
}));